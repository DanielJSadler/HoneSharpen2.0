import React from 'react';
import { TextInput } from '../../atoms';

interface Props {}

export const Homepage = (props: Props) => (
  <div className="bg-red-500 items-center justify-center w-full ">
    <h1 className="text-white text-lg text-center ">louis you a bitch</h1>
    <TextInput></TextInput>
  </div>
);
